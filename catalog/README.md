# Catalog layout

`catalog/rules/` is the source of truth for individual rules.

Each rule lives at:

`catalog/rules/<CATEGORY_CODE>/<FAMILY_CODE>/<RULE_ID>.json`

`catalog/master.json` is generated from these files and should not be hand-edited.
