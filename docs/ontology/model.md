# Ontology model

Each rule describes an inefficiency pattern.

Key fields:
- layer (primary)
- ontology.system_layers (related layers)

Canonical layers:
ai, architecture, code, data, network, process
